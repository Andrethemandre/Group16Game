package org.group16.Model;

void updatehealth(){

    if (player.getHealth() > 0 )
    
    {
        player.setHealth(play er.getHealth() - 1);
       
    }
}
@Override
Boolean isDead(){
    if (player.getHealth() == 0)
    {
        return true;
    }
    else
    {
        return false;
    }```
}
}